apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - path: patch.yaml
  # Inject AWS_REGION env into all Druid StatefulSets
  # (removed from base; only needed when using S3)
  - path: statefulset-env-patch.yaml
    target:
      kind: StatefulSet
